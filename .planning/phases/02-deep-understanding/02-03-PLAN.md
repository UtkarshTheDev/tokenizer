---
phase: 02-deep-understanding
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "User understands purpose of special tokens (BOS, EOS, PAD, UNK, FIM)"
    - "User can implement special token handling"
    - "User knows model-specific special token schemes"
  artifacts:
    - path: "src/specialTokens.ts"
      provides: "Special token management system"
    - path: "src/tokenizers/chatFormat.ts"
      provides: "Chat format tokenization (OpenAI, Claude styles)"
---

<objective>
Implement special tokens system - essential for production tokenizers and chat models.

Purpose: Understand how models handle conversation boundaries, padding, and unknown tokens
Output: Special token system with support for multiple chat formats
</objective>

<execution_context>
@/home/utkarsh/development/tokenizer/.planning/phases/02-deep-understanding/02-01-RESEARCH.md
</execution_context>

<context>
# Special Tokens Reference

## Common Special Tokens

| Token | Name | Purpose |
|-------|------|---------|
| `<pad>` | Padding | Batch processing, fixed sequence length |
| `<bos>` / `<s>` | Beginning of Sequence | Tell model input is starting |
| `<eos>` / `</s>` | End of Sequence | Tell model output is ending |
| `<unk>` | Unknown | Handle out-of-vocabulary words |
| `<|im_start|>` | (OpenAI) | Start of message |
| `<|im_end|>` | (OpenAI) | End of message |
| `<|endoftext|>` | (OpenAI) | End of document |
| `<think>` | (Claude) | Turn on thinking |
| `</think>` | (Claude) | Turn off thinking |

## Chat Format Examples

### OpenAI (GPT-4)
```
<|im_start|>system
You are a helpful assistant.<|im_end|>
<|im_start|>user
Hello!<|im_end|>
<|im_start|>assistant
Hi! How can I help?<|im_end|>
```

### Claude
```
\n\nHuman: Hello\n\n
Assistant: Hi! How can I help?\n\n
```

### Llama 3
```
<|begin_of_text|>system
You are a helpful assistant.<|eot_id|>
<|start_header_id|>user<|end_header_id|>

Hello<|eot_id|>
<|start_header_id|>assistant<|end_header_id|>

Hi! How can I help?<|eot_id|>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create special tokens manager</name>
  <files>src/specialTokens.ts</files>
  <action>
    Create src/specialTokens.ts:

    1. Define `SpecialToken` interface:
       ```typescript
       interface SpecialToken {
         token: string;
         tokenId: number;
         controlToken: string;  // e.g., "<|im_start|>"
       }
       ```

    2. Create `SpecialTokens` class with:
       - `vocabSize: number` (extends base vocab)
       - `tokens: Map<string, SpecialToken>` (token string -> info)
       - `reservedIds: Set<number>` (reserved token IDs)
       
    3. Implement methods:
       - `add(token: string, controlToken: string)` - Add special token
       - `getId(token: string): number` - Get token ID
       - `getToken(id: number): string` - Get token from ID
       - `isSpecial(id: number): boolean` - Check if ID is special
       - `encodeWithSpecials(text: string, baseEncoder): number[]` - Encode with special tokens

    4. Pre-define standard sets:
       - `openaiChatTokens()` - im_start, im_end, endoftext
       - `claudeTokens()` - Human:, Assistant:
       - `llama3Tokens()` - begin_of_text, eot_id, etc.
       - `basicTokens()` - bos, eos, pad, unk
  </action>
  <verify>Test: Add custom token, verify it gets unique ID</verify>
  <done>Special tokens can be added and retrieved by ID or string</done>
</task>

<task type="auto">
  <name>Task 2: Implement chat format tokenizer</name>
  <files>src/tokenizers/chatFormat.ts</files>
  <action>
    Create src/tokenizers/chatFormat.ts:

    1. Define `ChatMessage` interface:
       ```typescript
       interface ChatMessage {
         role: "system" | "user" | "assistant";
         content: string;
       }
       ```

    2. Create `ChatFormat` class supporting:
       - OpenAI format: <|im_start|>role\ncontent<|im_end|>
       - Claude format: \n\nHuman: content\n\n
       - Llama 3 format: <|begin_of_text|><|start_header_id|>role<|end_header_id|>\n\ncontent<|eot_id|>
       - Basic format: <|bos|>content<|eos|>

    3. Implement methods:
       - `format(messages: ChatMessage[], format: FormatType): string`
       - `tokenize(messages: ChatMessage[], tokenizer, format): number[]`
       - `detectFormat(sample: string): FormatType` - Auto-detect

    4. Handle special cases:
       - System message always first
       - Tool calls / function calling
       - Reasoning tokens (for Claude)
  </action>
  <verify>Test: Format messages in OpenAI style, verify tokens match expected</verify>
  <done>Chat messages correctly formatted for different model families</done>
</task>

<task type="auto">
  <name>Task 3: Update BPE to handle unknown tokens</name>
  <files>src/tokenizers/bpeWithSpecials.ts</files>
  <action>
    Modify your BPE to handle unknowns:

    1. Add `unknownToken: string` parameter to train/encode
    2. During encoding:
       - If token not in vocabulary AND unknownToken defined:
         - Split into subwords that ARE in vocabulary
         - If no subwords match, use unknown token
       - Return special ID for unknown

    3. Create `createWithSpecials(baseVocabSize: number, specialTokens: SpecialTokens)`:
       - Reserve IDs 0-255 for bytes
       - Reserve next N IDs for special tokens
       - Remaining for BPE merges

    This is how real tokenizers handle OOV words!
  </action>
  <verify>Test: Encode word not in training vocabulary - should handle gracefully</verify>
  <done>BPE handles unknown words without crashing</done>
</task>

</tasks>

<verification>
1. Can add/remove special tokens
2. Chat formats produce correct string output
3. Unknown words handled gracefully
4. Can tokenize full conversation with system/user/assistant messages
</verification>

<success_criteria>
- [ ] Special tokens manager working
- [ ] Supports OpenAI, Claude, Llama 3 chat formats
- [ ] Unknown token handling implemented
- [ ] User understands when to use special tokens
</success_criteria>

<output>
After completion, create `.planning/phases/02-deep-understanding/02-03-SUMMARY.md`
</output>
